# Meterpreter Dashboard Frontend

## ENV Variables
When running the docker image, it's possible to set environmental variables.

> **API_URL:** Backend URL target. Defaults to `https://localhost:3000`
